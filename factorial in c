#include <stdio.h>

int main(){
	 int n,i,j;
	 
	 printf("Enter num: \n");
	 scanf("%d", &n);
	 i = (n - 1);
	 
while ( i <= n ) {
	
	n = n * i;
	i = i - 1;
	
	if(i == 0){
		printf("the factorial is:  %d\n", n);
		break;
		
	}
	
}
	
	return 0;
}
